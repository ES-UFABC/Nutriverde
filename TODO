Authentication: 
    Basic-auth-with-JWT:
        [+] fix user register and login routes (sending a JWT OBJECT) 
        [+] password must be generated by a bcrypt method and then stored
        [+] Diggest a JWT from the user 
        [+] Store a JWT in browser
        [+] protect a Page with Login credentials 
    Auth-2-for-Google:
        [-] register
        [-] login
    Update-JWT:
        [] Bearer (Autorization)
        [] http only ( only server may diggesty )
        [] Refresh + Acess token

CIBadge:
    [] testes passados 
    [] Donwload 
    [] versionamento 
    [] procurar alguns legais (até 3 mesmo)

UnitTest:
    Model:
        [] Producer 
        [] User 
        [] Product
    Service:
        [] Producer 
        [] User 
        [] Product


bugs: 
    [+] lidar com o token invalidado no cliente-side (expirado) -- direcionar pro login
    [+] Bug com F5 na tela do profile (design está quebrando)
    [] inserir elementos já existentes ?  


ProducerEvo:
    [] preeencher o formulario de evolução (user -> Producer) para gerar um form de atualização de producer
    [] preeencher o formulario de cadastro (user ) para gerar um form de atualização de user
    [] 